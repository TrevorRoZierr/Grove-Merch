import BottomMenu from "./BottomMenu";
import RightMenu from "./RightMenu";

const MainMenu = () => {
  return (
    <div>
      <RightMenu />
      <BottomMenu />
    </div>
  );
};

export default MainMenu;
